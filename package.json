{
  "name": "wiki-publisher-action",
  "version": "1.0.0",
  "description": "GitHub Action to publish documentation to Wiki",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts",
    "dev": "tsup src/index.ts --format cjs --watch",
    "lint": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/exec": "^1.1.1",
    "@actions/github": "^5.1.1",
    "glob": "^10.3.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "tsup": "^8.0.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
